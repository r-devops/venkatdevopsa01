- name: Demo on variables 
  hosts: all
  vars: 
    URL: play.example.com 
  tasks: 
    - name: Print Hello 
      ansible.builtin.debug: 
        msg: URL= {{ URL}}

- name: Demo on variables - from file
  hosts: webserver
  vars_files: 
    - sample-vars.yml  
  tasks: 
    - name: Print Hello 
      ansible.builtin.debug: 
        msg: URL= {{ URL}}        